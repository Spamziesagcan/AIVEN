# Ollive Chat — Next.js Frontend

Minimal Next.js scaffold for the Ollive chat frontend. Uses a modern, vibrant theme and a separate `backend` API.

Run locally:

```bash
cd frontend-next
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE` to your backend API base URL if it is not `http://localhost:8000`.
